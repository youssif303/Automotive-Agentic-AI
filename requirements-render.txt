# Tell pip to fetch CPU-only PyTorch wheels (avoids downloading 2.5GB of NVIDIA CUDA libraries)
--extra-index-url https://download.pytorch.org/whl/cpu
torch

# PDF parsing
pymupdf>=1.24.0

# Embeddings & vector search
sentence-transformers>=3.0.0
faiss-cpu>=1.8.0

# Web server
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
websockets>=12.0
python-multipart>=0.0.9

# Cloud inference client
requests>=2.31.0
