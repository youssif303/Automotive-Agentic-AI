# Ultra-lightweight dependencies for Render Free Tier (< 90MB RAM total)
# Uses ONNX Runtime (fastembed) instead of heavy PyTorch
fastembed>=0.8.0
faiss-cpu>=1.8.0
pymupdf>=1.24.0

# Web server
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
websockets>=12.0
python-multipart>=0.0.9

# Cloud inference client
requests>=2.31.0
click>=8.1.0
